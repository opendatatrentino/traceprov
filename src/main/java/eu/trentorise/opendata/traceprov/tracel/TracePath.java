package eu.trentorise.opendata.traceprov.tracel;

/**
 * 
 * A query in a json tree, inspired by JsonPath / XPath, with the difference it
 * is still valid ES6 Javascript, though. More info in the docs.
 *
 */
abstract class TracePath extends ATraceExpr {
    
}
